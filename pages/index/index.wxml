<!--index.wxml-->
<view class="containar">
  <map id="BaiduMap">
    latitude="{{latitude}}"
    longitude="{{longitude}}"
    scale="{{scale}}"
    controls="{{controls}}"
    show=location/>
</view>